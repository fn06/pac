(menhir
 (modules parser))

(ocamllex lexer)

(library
 (name pac)
 (libraries pubgrub opam-format)
 (modules ast lexer parser core concurrent import deb_version deb_packages debian opam_repo opam_frontend))
