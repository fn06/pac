(menhir
 (modules parser))

(ocamllex lexer)

(library
 (name pac)
 (libraries pubgrub opam-format yojson)
 (modules ast lexer parser core concurrent import deb_version deb_packages debian opam_repo opam_frontend semver cargo_index cargo_frontend))
